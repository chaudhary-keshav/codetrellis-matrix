## ⚠️ MANDATORY: READ CONTEXT FIRST - DO NOT SKIP

I'm providing a CodeTrellis Matrix file that contains a COMPLETE snapshot of my Angular project. This matrix includes:

- All components with their @Input/@Output contracts
- All interfaces with property schemas
- All services with injections and methods
- All stores with state shapes
- All routes
- All WebSocket events (incoming/outgoing)
- All HTTP API endpoints with base URLs
- Best practices for this stack

**INSTRUCTIONS:**

1. READ the entire CodeTrellis Matrix BEFORE doing ANY file exploration
2. USE the matrix data as your PRIMARY source for understanding the UI project
3. ONLY read actual source files if you need implementation details not in the matrix
4. When debugging sync issues, compare matrix [HTTP_API] and [WEBSOCKET_EVENTS] against backend

The matrix is attached below. Acknowledge that you've read it by listing:

- Number of components
- Number of interfaces
- Number of services
- Number of stores
- Number of HTTP endpoints
- Number of WebSocket events

Then proceed with the task.
